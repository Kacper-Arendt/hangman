import { Form } from "coreUI";

export const Login = () => (
  <Form
    email
    buttonValue="Login"
    onSubmit={() => {}}
    linkPath="/"
    linkValue="Register"
    linkText="Need an account?"
  />
);
